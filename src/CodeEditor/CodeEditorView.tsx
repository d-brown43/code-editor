import * as React from "react";
import styles from './CodeEditor.module.scss';

export default () => {
    return (
        <div className={styles.container}>
            Hello World
        </div>
    )
};
